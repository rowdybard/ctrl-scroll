{
  "name": "ctrl-scroll",
  "version": "2.0.0",
  "description": "Reddit → AI → Static Site Generator V2",
  "private": true,
  "scripts": {
    "build": "pnpm --filter @ctrlscroll/shared build && pnpm --filter @ctrlscroll/web build",
    "dev": "turbo run dev",
    "start": "cd apps/web && NODE_ENV=production next start",
    "test": "turbo run test",
    "test:shared": "turbo run test --filter=@ctrlscroll/shared",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "type-check": "turbo run type-check"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "turbo": "^1.11.2",
    "typescript": "^5.3.0",
    "eslint": "^8.55.0",
    "prettier": "^3.1.0"
  },
  "dependencies": {
    "next": "^14.0.4"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}